using System;
using System.Text.RegularExpressions;

namespace LeetCode
{
    class IntIdentity
    {
        public void mySolution(string str) 
        {
            private static readonly Int32 _Max = Int32.MaxValue;
            private static readonly Int32 _Min = Int32.MinValue;
            string defultReturn = "0";
            //正向表达式模式;
            Regex _isAllNull = new Regex("[]");
            Regex _isAllChar = new Regex("* ");
            Regex _isNum     = new Regex("");
            


            
        }

    }
}